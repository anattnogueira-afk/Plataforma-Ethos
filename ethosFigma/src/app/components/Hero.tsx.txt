interface HeroProps {
  onStartQuiz: () => void;
}

export function Hero({ onStartQuiz }: HeroProps) {
  return (
    <section className="py-16 sm:py-20 lg:py-32 bg-background" aria-labelledby="hero-title">
      <div className="container mx-auto px-4 sm:px-6 max-w-4xl text-center">
        <h1 id="hero-title" className="mb-6 px-4">
          Simulador de Decisões Éticas em Telemedicina
        </h1>
        <p className="mb-12 text-foreground max-w-2xl mx-auto px-4" style={{ fontSize: '18px', lineHeight: '1.6' }}>
          Identifique dilemas éticos em sua prática profissional e compreenda 
          o impacto social de suas decisões em ambientes de telemedicina.
        </p>
        <button
          onClick={onStartQuiz}
          className="px-8 py-4 bg-primary text-primary-foreground hover:opacity-90 transition-opacity focus:outline-none focus:ring-2 focus:ring-ring focus:ring-offset-4"
          style={{ borderRadius: 'var(--radius)', fontSize: '16px' }}
          aria-label="Iniciar simulação de ética em telemedicina"
        >
          Iniciar Simulação
        </button>
      </div>
    </section>
  );
}